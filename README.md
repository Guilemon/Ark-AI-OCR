# Ark-AI-OCR
Extract structured data from documents in seconds. No manual data entry, No errors.


**Features:**
- OCR document scanning
- Automatic data extraction (names, dates, etc.)
- API endpoint for batch processing
